# AGENTS.md

本文件定义自动化编码 Agent 和协作者的通用工作方式。这里只记录长期稳定的协作、设计和验证约束，不记录业务模块、当前架构、技术栈、命令清单或临时决策；这些内容应放入 README、spec 或 plan。

## 工作原则

- 修改前先阅读相关代码和文档，确认任务范围、依赖与接口影响。
- 优先遵循仓库现有模式，保持改动聚焦，不夹带无关重构或格式化。
- 保留他人和既有未提交修改；未经明确要求，不执行破坏性 Git 操作。
- 按 plan 分步实施时，每个步骤完成后先保持该步骤变更为未暂存状态，供人工审阅。
- 仅在收到进入后续步骤的明确指示后、且开始编辑后续步骤前，先检查 `git diff` 和 `git status`，再暂存上一项已完成且验证通过的步骤；只暂存该步骤及必要协作文档，不夹带未完成或无关修改，并检查、报告 cached diff。
- 暂存不等于提交；未经明确要求，不提交、不推送、不创建 PR，也不触发范围外的外部写操作。
- 只有阻塞性选择才向用户提问；其余情况采用保守、可解释且符合现有约定的判断继续推进。
- 修改跨模块或外部可见接口时，同步更新相关文档与测试。

## 文档规则

- 面向人的文档使用中文；代码标识、命令、路径和配置键保持原文。
- 功能实现、架构调整或跨模块改动开始前，应有对应的中文 spec 和 plan；小范围文档修正可以直接完成。
- spec 描述需求、约束、接口和非目标，plan 描述实施顺序、涉及文件与验证方式，二者不复制大段实现代码。
- 文档中不保留 `TODO`、`TBD`、`待补充` 等占位内容；暂不实施的内容明确列入“非本阶段范围”。
- 历史文档保持历史事实；新决策通过新文档或明确的修订记录表达。

## 代码设计

- 可读性优先于技巧性。名称表达领域含义，主流程尽量保持从上到下可读。
- 模块应具有小而稳定的接口，并隐藏复杂实现；依赖方向、资源所有权、生命周期和错误语义必须明确。
- 只在存在真实变化点、重复逻辑或显著复杂度时引入抽象；避免透传包装、投机性扩展和设计模式堆叠。
- 在真实 seam 注入依赖，让调用方与测试通过同一接口观察行为，不为测试泄漏内部实现细节。
- 优先使用语言和库提供的可靠资源管理方式，避免共享可变全局状态和隐式生命周期。
- 注释说明约束、原因和不明显的权衡，不复述代码表面行为。

## 实施过程报告

- 每个计划步骤开始编辑前，先报告精确文件路径及新增、修改、删除或重命名动作；能够确定具体文件时，不使用目录通配符代替。
- 同时列出涉及的标识符，包括相关类型、函数、成员变量、配置键、日志字段，以及职责、接口、生命周期、并发或算法发生变化的重要局部变量。
- 重命名或迁移使用 `旧名称 -> 新名称` 表示并说明原因。代码勘察导致计划变化时，先更新当步清单，再开始编辑。
- 每步完成后报告实际文件与标识符、接口和所有权影响、验证结果，以及与开始前清单的差异。

推荐格式：

```text
步骤 N：名称

涉及文件
- path/file.ext：新增 | 修改 | 删除 | 重命名

涉及标识符
- TypeName / function_name / variable_name：动作与职责

接口与所有权影响
- 调用方、返回值、错误语义、创建方、持有方和释放方

验证
- 测试文件、命令和结果
```

## 验证与交付

- 验证范围与改动风险相匹配；共享接口和跨模块行为需要更完整的测试。
- 完成后运行相关格式、静态检查和测试。因环境限制无法执行时，明确说明原因和剩余风险。
- 最终报告聚焦实际结果、关键文件、验证结论和未解决风险，不把计划描述成已完成事实。
- 不留下临时模型、缓存、截图、日志或调试产物，除非它们本身就是明确交付物。
