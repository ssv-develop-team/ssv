# 安全帽模型验证说明

## 模型文件

本阶段验证 `models/comp-*.pt`。这些文件是 Ultralytics/PyTorch 权重，不能被 `ssvinfer` 直接加载。

现有工程实时链路通过 `ssvinfer` 和 ONNX Runtime 加载 ONNX 模型。安全帽 `.pt` 需要先完成离线验证，再导出 ONNX 后接入工程 smoke。

## 离线验证

只验证模型能加载并读取类别表：

```bash
uv run --with ultralytics python scripts/model/verify_helmet_models.py --models-dir models
```

带图片或视频源验证：

```bash
uv run --with ultralytics python scripts/model/verify_helmet_models.py --models-dir models --source /path/to/validation/images
```

输出摘要默认写入：

```text
artifacts/model-verification/helmet-model-summary.json
```


## 本次验证结果

`models/comp-*.pt` 均可加载，类别表一致：

```text
0: helmet
1: head
```

已导出工程接入候选模型：

```text
models/comp-2-freeze10.onnx
```

导出的 ONNX 输出形状为 `(1, 300, 6)`，`ssvinfer` 已增加 Nx6 输出解析分支，按 `[x1, y1, x2, y2, score, class_id]` 写入检测元数据。


## 实施结果总结

本次 T2 安全帽模型验证与接入计划已完成工程落地。原实时链路确认仍以 ONNX Runtime 加载 ONNX 模型，`models/` 下的 `.pt` 权重只用于离线验证和导出，不直接进入 `ssvinfer` 运行时。

已完成的主要改动：

- 新增 `scripts/model/verify_helmet_models.py`，用于加载 `models/comp-*.pt`、读取模型 `names`、可选执行图片或视频源推理，并可将选定 `.pt` 导出为 ONNX。
- 新增 `config/model-labels/coco80.txt` 和 `config/model-labels/helmet.txt`。其中安全帽模型类别顺序为 `helmet`、`head`，与本次验证的 `.pt` 模型一致。
- 选定 `models/comp-2-freeze10.pt` 作为工程接入模型，并导出 `models/comp-2-freeze10.onnx`。
- `ssvinfer` 增加 `label-map` 属性，检测类别名从固定 COCO 表扩展为运行时类别表；`target-class` 为空时输出全部类别。
- `ssvinfer` 增加对 Ultralytics ONNX Nx6 输出的解析，支持 `(1, 300, 6)` 格式。
- `ssvinfer` 仅保留 `runtime` 和 `device` 属性，支持显式选择推理运行时与设备；CUDA device id 通过 `SSV_INFER_CUDA_DEVICE_ID` 环境变量控制。
- `scripts/build.sh` 和 `scripts/lib.sh` 支持 `SSV_ONNXRUNTIME_FLAVOR=cpu|gpu`，CPU 和 GPU 版 ONNX Runtime 分别放在 `.deps/onnxruntime` 和 `.deps/onnxruntime-gpu`。
- `scripts/lib.sh` 会自动发现 uv 虚拟环境中的 NVIDIA wheel 动态库目录，解决 GPU 版 ONNX Runtime 找不到 CUDA/cuDNN 运行库的问题。
- `scripts/pipeline.sh` 已将模型、类别表、目标类别、推理设备和 CUDA 策略传入 `ssvinfer`。
- 修复 `./ssv run --display` 在 X11/GTK 显示环境下可能无法弹窗的问题：脚本内部显示开关改为 `SHOW_DISPLAY`，避免覆盖系统 `DISPLAY` 环境变量。

本次排查中确认的关键结论：

- 看到 `nvh264dec` 或 `GstCudaContext` 只能说明视频解码使用了 GPU，不代表模型推理使用 GPU。
- 判断模型推理是否使用 GPU，应看 `ssvinfer` 的 ONNX Runtime provider 日志；当 `SSV_RUNTIME_BACKEND=onnx` 且 `SSV_RUNTIME_DEVICE=gpu` 时，如果 CUDA provider 不可用，pipeline 会直接启动失败。
- `gtksink` 可使用统一 tee 显示链路；不需要为 `gtksink` 长期保留单链路显示特例。

已执行并通过的验证包括：

```bash
bash tests/ssv_cli_test.sh
./ssv build
meson test -C build
SSV_RUNTIME_BACKEND=onnx SSV_RUNTIME_DEVICE=gpu SSV_CHECK_TIMEOUT=8s \
  bash scripts/pipeline.sh --smoke --skip-build
```

`./ssv test` 未作为本次最终验收依据，因为 Python Agent 开发依赖中缺少 `pytest` 时会失败；该问题属于 Agent 测试环境，不影响本次 T2 推理链路改动。

推荐的安全帽模型运行配置：

```env
SSV_MODEL_PATH=models/comp-2-freeze10.onnx
SSV_LABEL_MAP=config/model-labels/helmet.txt
SSV_TARGET_CLASS=
SSV_ONNXRUNTIME_FLAVOR=gpu
SSV_RUNTIME_BACKEND=onnx
SSV_RUNTIME_DEVICE=gpu
SSV_INFER_CUDA_DEVICE_ID=0
```

使用以上配置后运行：

```bash
./ssv build
./ssv run --display
```

## ONNX 导出

选择一个候选模型导出：

```bash
uv run --with ultralytics python scripts/model/verify_helmet_models.py \
  --models-dir models \
  --source /path/to/validation/images \
  --export models/comp-2-freeze10.pt
```

导出后确认 `config/model-labels/helmet.txt` 与 JSON 摘要中该模型的 `names` 顺序一致。`config/model-labels/helmet.txt` 每行一个类别名，行号对应模型输出的 `class_id`。

## 工程 smoke

```bash
SSV_MODEL_PATH=models/comp-2-freeze10.onnx \
SSV_LABEL_MAP=config/model-labels/helmet.txt \
SSV_TARGET_CLASS=head \
./ssv run --no-display
```

## 推理设备

默认配置为 CPU：

```bash
SSV_RUNTIME_BACKEND=onnx
SSV_RUNTIME_DEVICE=cpu
SSV_INFER_CUDA_DEVICE_ID=0
SSV_ONNXRUNTIME_FLAVOR=cpu
```

`backend=onnx` 且 `device=cpu` 只使用 CPUExecutionProvider。`backend=onnx` 且 `device=gpu` 只尝试 CUDAExecutionProvider；如果当前运行库或 CUDA/cuDNN 环境不可用，`ssvinfer` 会直接启动失败。CUDA device id 不通过 YAML 或插件属性指定，而是读取 `SSV_INFER_CUDA_DEVICE_ID`。日志会打印 requested runtime 和 active provider，用来区分 GPU 视频解码与 GPU 模型推理。

使用 GPU 推理时先切换 ONNX Runtime GPU 包并重新构建：

```bash
SSV_ONNXRUNTIME_FLAVOR=gpu ./ssv build
```

生产环境如果必须使用 GPU，可设置：

```bash
SSV_RUNTIME_BACKEND=onnx
SSV_RUNTIME_DEVICE=gpu
SSV_INFER_CUDA_DEVICE_ID=0
SSV_ONNXRUNTIME_FLAVOR=gpu
```

此时 CUDA Execution Provider 不可用会导致 pipeline 启动失败。

如果只想观察全部类别，将 `SSV_TARGET_CLASS` 设为空字符串：

```bash
SSV_TARGET_CLASS="" ./ssv run --no-display
```

## 常见失败

- `.pt` 直接传给 `SSV_MODEL_PATH`：`ssvinfer` 会通过 ONNX Runtime 加载，不能识别 PyTorch 权重。
- `label-map class count mismatch`：ONNX 输出类别数与 label map 行数不一致，需要检查导出模型和类别表是否配套。
- `target-class not found in label-map`：`SSV_TARGET_CLASS` 不在当前类别表中，需要使用类别表里的原始英文类别名。
- 缺少 `ultralytics`：离线验证脚本无法加载 `.pt`，需要先安装 Python 推理依赖。
- `CUDAExecutionProvider unavailable`：当前 ONNX Runtime 包不是 GPU 版，或 NVIDIA driver、CUDA、cuDNN 运行时不匹配。`SSV_RUNTIME_BACKEND=onnx` 且 `SSV_RUNTIME_DEVICE=gpu` 时 pipeline 会启动失败。
