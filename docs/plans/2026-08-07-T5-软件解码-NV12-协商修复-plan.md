# 软件解码 NV12 协商修复计划

## 步骤 1：编写 spec/plan

- 新增 `docs/specs/2026-08-07-T5-软件解码-NV12-协商修复-spec.md` 与
  `docs/plans/2026-08-07-T5-软件解码-NV12-协商修复-plan.md`。

## 步骤 2：修改 pipeline builder

涉及文件
- `runner/pipeline/ssv_pipeline_builder.cpp`：修改

涉及标识符
- `pipeline_internal::resolve_topology`：软件解码时在 `h264-decoder` 后插入
  `videoconvert` 阶段 `decode-format`

## 步骤 3：更新 pipeline builder 测试

涉及文件
- `runner/pipeline/tests/test_ssv_pipeline_builder.cpp`：修改

## 步骤 4：验证

- 运行相关 Meson 测试。
- 本机执行 `./ssv run --display --overlay` 验证不再 `not-negotiated`。
