# T1/T2 日志人类可读输出计划

## 步骤 1：扩展 logger 格式契约

- 修改 `runner/observability/ssv_event_log.hpp`，新增 `SsvEventLogFormat` 和
  `SsvEventLogOptions::format`。
- 保持 `Structured` 默认值，确保现有调用方和测试无需改变即可获得原有输出。

## 步骤 2：实现 pretty renderer 与终端选择

- 修改 `runner/observability/ssv_event_log.cpp`，增加事件分组、缩进、字段宽度和 p50/p95
  展示；结构化编码继续作为截断和机器输出的基准。
- 修改 `runner/main.cpp`，仅在 stderr 是终端时选择 `Pretty`，重定向和管道保持
  `Structured`。

## 步骤 3：测试和验收

- 修改 `runner/observability/tests/test_ssv_event_log.cpp`，覆盖 runtime resolved、
  inference stats 的 pretty 文本以及默认结构化输出。
- 运行 logger、runner、Meson 全量测试和 `git diff --check`。
