#!/usr/bin/env bash

# User-facing managed dependency defaults. Provider-specific package details
# stay in the provider that owns them.
SSV_DEPS_DEFAULT_ONNXRUNTIME_VERSION="1.25.1"
SSV_DEPS_DEFAULT_OPENCV_VERSION="4.10.0"
